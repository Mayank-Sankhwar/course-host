import { FormEvent, useEffect, useState } from 'react';
import { courseApi, type Course } from './course-api';
import { LessonManager } from './lesson-manager';
import { CourseDiscussion } from './course-discussion';
import { EnrollmentManager } from './enrollment-manager';
import { ActivityManager } from './activity-manager';

const emptyCourse = { title: '', description: '', category: '' };
type DetailTab = 'metadata' | 'lessons' | 'learners' | 'activity' | 'discussion';
const statusClass = (status: string) => `status-badge status-${status.toLowerCase().replaceAll('_', '-')}`;

export function CourseManager({ initialActivity = false }: { initialActivity?: boolean }) {
  const [courses, setCourses] = useState<Course[]>([]); const [form, setForm] = useState(emptyCourse);
  const [selected, setSelected] = useState<Course | null>(null);
  const [tab, setTab] = useState<DetailTab>(initialActivity ? 'activity' : 'metadata'); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); const [saving, setSaving] = useState(false); const [transitioning, setTransitioning] = useState(false);
  async function loadCourses() { setLoading(true); try { const response = await courseApi.list(); setCourses(response.courses); } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to load courses.'); } finally { setLoading(false); } }
  useEffect(() => { void loadCourses(); }, []);
  async function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    setError(null);
    setSaving(true);
    try {
      const response = selected
        ? await courseApi.update(selected.id, form)
        : await courseApi.create(form);
      setSelected(response.course);
      setForm({ title: response.course.title, description: response.course.description, category: response.course.category });
      await loadCourses();
    } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to save course.'); } finally { setSaving(false); }
  }
  async function transition(action: 'publish' | 'archive' | 'restore') { if (!selected) return; setError(null); setTransitioning(true); try { const { course } = await courseApi.transition(selected.id, action); setSelected(course); await loadCourses(); } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to update course status.'); } finally { setTransitioning(false); } }
  function open(course: Course, nextTab: DetailTab = initialActivity ? 'activity' : 'metadata') { setSelected(course); setTab(nextTab); setForm({ title: course.title, description: course.description, category: course.category }); }
  if (selected) return <section className="card course-detail"><div className="course-detail-header"><div><button className="back-button" onClick={() => { setSelected(null); setForm(emptyCourse); }}>← All courses</button><p className="eyebrow">Course management</p><h2>{selected.title}</h2><p className="course-detail-meta"><span>{selected.category}</span><span>Created {new Date(selected.createdAt).toLocaleDateString()}</span></p></div><div className="course-state"><span className={statusClass(selected.status)}>{selected.status}</span>{selected.status === 'ARCHIVED' && <p className="archived-note">This course is archived. Its lessons, enrollments, and history remain preserved.</p>}</div></div>{error && <p role="alert">{error}</p>}<nav className="detail-tabs" aria-label="Course management sections">{(['metadata', 'lessons', 'learners', 'activity', 'discussion'] as DetailTab[]).map((item) => <button key={item} aria-current={tab === item ? 'page' : undefined} onClick={() => setTab(item)}>{item[0].toUpperCase() + item.slice(1)}</button>)}</nav>{tab === 'metadata' && <section className="metadata-panel"><div className="section-heading"><div><h3>Course metadata</h3><p className="helper-text">Update the information learners see in the course catalogue.</p></div></div><form onSubmit={submit}><label>Title <input value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} required disabled={saving} /></label><label>Description <textarea value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} required disabled={saving} /></label><label>Category <input value={form.category} onChange={(event) => setForm({ ...form, category: event.target.value })} required disabled={saving} /></label><button type="submit" disabled={saving}>{saving ? 'Saving metadata…' : 'Save metadata'}</button></form><div className="lifecycle-actions"><div><h4>Course lifecycle</h4><p className="helper-text">Status changes preserve existing course data and learner history.</p></div><div className="actions">{selected.status === 'DRAFT' && <button className="button-primary" disabled={transitioning} onClick={() => void transition('publish')}>{transitioning ? 'Publishing…' : 'Publish'}</button>}{selected.status === 'PUBLISHED' && <button className="danger" disabled={transitioning} onClick={() => { if (window.confirm('Archive this course? Enrollments and progress will be preserved.')) void transition('archive'); }}>{transitioning ? 'Archiving…' : 'Archive'}</button>}{selected.status === 'ARCHIVED' && <button className="button-primary" disabled={transitioning} onClick={() => void transition('restore')}>{transitioning ? 'Restoring…' : 'Restore'}</button>}</div></div></section>}{tab === 'lessons' && <LessonManager courseId={selected.id} courseTitle={selected.title} />}{tab === 'learners' && <EnrollmentManager courseId={selected.id} courseTitle={selected.title} />}{tab === 'activity' && <ActivityManager courseId={selected.id} courseTitle={selected.title} />}{tab === 'discussion' && <CourseDiscussion courseId={selected.id} />}</section>;
  return <section className="card"><div className="page-heading"><div><p className="eyebrow">Instructor workspace</p><h2>Courses</h2><p>Manage your course library, lessons, learners, and activity.</p></div></div>{error && <p role="alert">{error}</p>}<div className="subsection create-course-panel"><div><h3>Create course</h3><p className="helper-text">Start with a draft, then add lessons before publishing.</p></div><form onSubmit={submit}><label>Title <input value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} required disabled={saving} /></label><label>Description <textarea value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} required disabled={saving} /></label><label>Category <input value={form.category} onChange={(event) => setForm({ ...form, category: event.target.value })} required disabled={saving} /></label><button type="submit" disabled={saving}>{saving ? 'Creating course…' : 'Create draft course'}</button></form></div>{loading ? <p className="loading-state">Loading your courses…</p> : courses.length === 0 ? <p className="empty">No courses yet. Create your first draft course above.</p> : <div className="course-list instructor-course-list">{courses.map((course) => <article className="course-card" key={course.id}><div className="course-card-header"><strong>{course.title}</strong><span className={statusClass(course.status)}>{course.status}</span></div><p>{course.description}</p><p className="meta">{course.category} <span>·</span> created {new Date(course.createdAt).toLocaleDateString()}{course.enrollmentCount === undefined ? '' : ` · ${course.enrollmentCount} enrollments`}</p><div className="actions"><button className="button-primary" onClick={() => open(course)}>Manage course</button><button onClick={() => open(course)}>Edit metadata</button></div></article>)}</div>}</section>;
}
